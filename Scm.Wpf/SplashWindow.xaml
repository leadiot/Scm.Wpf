<Window
    x:Class="Com.Scm.SplashWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Topmost="True"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStyle="None"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterScreen"
    Title="Scm.Net"
    Icon="/Resources/logo.ico"
    Height="300" Width="500"
    Loaded="Window_Loaded">
    <Border CornerRadius="5" BorderThickness="2" BorderBrush="LightGray">
        <Border.Background>
            <ImageBrush ImageSource="/Resources/splash.png" Stretch="Fill"/>
        </Border.Background>
        <Grid>
            <!-- 启动屏内容：Logo + 进度条 + 提示文本 -->
            <StackPanel x:Name="PlLogo" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20 0 20 20" Width="300">
                <Image Source="/Resources/logo.png" Width="96" Height="96"/>
                <TextBlock Text="Nas.Net" Foreground="White" FontSize="24" TextAlignment="Center" Margin="0"/>
                <TextBlock Text="您的私有云盘" Foreground="White" FontSize="16" TextAlignment="Center" Margin="20 10 20 20"/>
                <ProgressBar x:Name="progressBar" Style="{StaticResource ProgressBarDanger}" Value="100" Maximum="200" IsIndeterminate="True"/>
                <TextBlock x:Name="statusText" Text="初始化中..." Foreground="#333333" />
            </StackPanel>
            <DockPanel x:Name="PlInfo" Visibility="Collapsed" Margin="12">
                <StackPanel DockPanel.Dock="Bottom">
                    <Button x:Name="BtExit" Click="BtExit_Click" Style="{StaticResource ButtonPrimary}">关闭</Button>
                </StackPanel>
                <Border Margin="0 0 0 6">
                    <TextBox x:Name="TbInfo" TextWrapping="Wrap" AcceptsReturn="True" AcceptsTab="True" IsReadOnly="True" VerticalScrollBarVisibility="Auto" Margin="6"></TextBox>
                </Border>
            </DockPanel>
        </Grid>
    </Border>
</Window>
