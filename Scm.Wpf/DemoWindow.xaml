<hc:Window x:Class="Com.Scm.Wpf.DemoWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           xmlns:hc="https://handyorg.github.io/handycontrol"
           mc:Ignorable="d"
           Title="DemoWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Path=Items}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="耳标" Binding="{Binding Path=Tag}"/>
                <DataGridTextColumn Header="找到" Binding="{Binding Path=Find}"/>
                <DataGridTextColumn Header="匹配" Binding="{Binding Path=Match}"/>
                <DataGridTextColumn Header="打印" Binding="{Binding Path=Ocr}"/>
                <DataGridTextColumn Header="标1" Binding="{Binding Path=OcrPre}"/>
                <DataGridTextColumn Header="标2" Binding="{Binding Path=OcrEnd}"/>
                <DataGridTextColumn Header="图片" Binding="{Binding Path=File}"/>
                <DataGridTextColumn Header="提示" Binding="{Binding Path=Message}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button x:Name="BtDemo" Click="BtDemo_Click">Demo</Button>
            <Label Content="{Binding Path=Message}" HorizontalAlignment="Stretch"/>
        </StackPanel>
    </Grid>
</hc:Window>
