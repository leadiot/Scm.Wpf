<UserControl x:Class="Com.Scm.Views.About.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="6"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>
    <Border Background="White" BorderThickness="0" BorderBrush="LightGray" CornerRadius="3">
        <DockPanel>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="BtVersion" Style="{StaticResource ButtonPrimary}" Width="240" Margin="12" Click="BtVersion_Click">检测更新</Button>
            </StackPanel>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Margin="6">
                <TextBlock TextWrapping="Wrap" Style="{StaticResource TextBlockTitleBold}" Text="{Binding AppName}"></TextBlock>
                <TextBlock x:Name="TbIntroduction" TextWrapping="Wrap" Text="{Binding Introduction}"></TextBlock>
                <TextBlock TextWrapping="Wrap">当前版本：
                    <Run Name="HrVersion" Text="{Binding AppVersion}"></Run>
                </TextBlock>
                <TextBlock TextWrapping="Wrap">发行日期：
                    <Run Name="HrRelease" Text="{Binding AppRelease}"></Run>
                </TextBlock>
                <TextBlock TextWrapping="Wrap">项目地址：
                <Hyperlink x:Name="HlProject" NavigateUri="{Binding AppProject}" Click="HlProject_Click" ToolTip="点击访问项目">
                    <Run Name="HrProject" Text="{Binding AppProject}"></Run>
                </Hyperlink>
                </TextBlock>
                <TextBlock TextWrapping="Wrap">网站首页：
                <Hyperlink x:Name="HlWebsite" NavigateUri="{Binding AppWebsite}" Click="HlWebsite_Click" ToolTip="点击访问网站">
                    <Run Name="HrWebsite" Text="{Binding AppWebsite}"></Run>
                </Hyperlink>
                </TextBlock>
                <TextBlock TextWrapping="Wrap">联系作者：
                <Hyperlink x:Name="HlEmail" NavigateUri="http://www.c-scm.net" Click="HlEmail_Click" ToolTip="点击发送邮件">
                    <Run Name="HrEmail" Text="{Binding AppEmail}"></Run>
                </Hyperlink>
                </TextBlock>
                <TextBlock TextWrapping="Wrap">QQ沟通群：
                <Hyperlink x:Name="HlQchat" NavigateUri="http://www.c-scm.net" Click="HlQchat_Click" ToolTip="点击复制群号">
                    <Run Name="HrQchat" Text="{Binding AppQchat}"></Run>
                </Hyperlink>
                </TextBlock>
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>
