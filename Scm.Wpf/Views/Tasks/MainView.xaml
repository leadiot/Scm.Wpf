<UserControl x:Class="Com.Scm.Wpf.Views.Tasks.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" Width="140" ItemsSource="{Binding Path=TaskList}" SelectedItem="{Binding Path=Task}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=Name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <DockPanel Grid.Column="1">
            <StackPanel DockPanel.Dock="Bottom" HorizontalAlignment="Right">
                <Button x:Name="BtRun" Content="运行" Click="BtRun_Click"/>
            </StackPanel>
            <TextBox x:Name="TbLog"
                     AcceptsReturn="True"
                     AcceptsTab="True" 
                     IsReadOnly="True" 
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Hidden"
                     Text="{Binding Path=Logs}"/>
        </DockPanel>
    </Grid>
</UserControl>
