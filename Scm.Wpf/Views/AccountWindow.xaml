<hc:Window
    x:Class="Com.Scm.Views.AccountWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    mc:Ignorable="d"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterScreen"
    Title="用户信息"
    Icon="/Resources/logo.ico"
    Height="360" Width="260">
    <hc:Card
        BorderThickness="1"
        Effect="{StaticResource EffectShadow2}"
        Margin="8">
        <Border CornerRadius="4,4,0,0" Style="{StaticResource BorderClip}">
            <Border Width="80" Height="80" CornerRadius="40" BorderThickness="5" BorderBrush="LightGray" Background="White">
                <Image x:Name="PbIcon" Width="64" Height="64" Source="/Resources/avatar.png">
                    <Image.Clip>
                        <EllipseGeometry RadiusX="32" RadiusY="32" Center="32,32"/>
                    </Image.Clip>
                </Image>
            </Border>
        </Border>
        <hc:Card.Footer>
            <StackPanel Margin="10">
                <TextBlock x:Name="TbName" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Style="{StaticResource TextBlockSubTitle}" Text="恭喜" HorizontalAlignment="Left"/>
                <TextBlock x:Name="TbCode" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Style="{StaticResource TextBlockTitleBold}" Text="恭喜" Margin="0,6,0,0" HorizontalAlignment="Left"/>
            </StackPanel>
        </hc:Card.Footer>
    </hc:Card>
</hc:Window>
