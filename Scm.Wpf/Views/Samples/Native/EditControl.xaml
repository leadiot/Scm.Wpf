<UserControl x:Class="Com.Scm.Wpf.Views.Samples.Native.EditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical">
        <hc:TextBox hc:InfoElement.ShowClearButton="True" hc:InfoElement.Title="编码" Text="{Binding Path=codec}"/>
        <hc:TextBox hc:InfoElement.ShowClearButton="True" hc:InfoElement.Title="名称" Text="{Binding Path=namec}"/>
        <hc:TextBox
            Text="{Binding Path=remark}"
            AcceptsReturn="True"
            AcceptsTab="True"
            TextWrapping="Wrap"
            hc:InfoElement.ShowClearButton="True"
            hc:InfoElement.Title="备注"/>
    </StackPanel>
</UserControl>
