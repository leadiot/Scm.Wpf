<UserControl x:Class="Com.Scm.Wpf.Views.Uc.UcPageData"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <Grid DockPanel.Dock="Bottom" Margin="5 5 5 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <hc:Pagination MaxPageCount="10" PageIndex="1" IsJumpEnabled="True" Margin="0">
                <hc:Interaction.Triggers>
                    <hc:EventTrigger EventName="PageUpdated">
                        <hc:EventToCommand  PassEventArgsToCommand="True" />
                    </hc:EventTrigger>
                </hc:Interaction.Triggers>
            </hc:Pagination>
            <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                <hc:ButtonGroup>
                    <RadioButton Content="列表" ToolTip="列表" IsChecked="True"/>
                    <RadioButton Content="单页" ToolTip="单页"/>
                </hc:ButtonGroup>
                <Button Content="刷新" ToolTip="刷新"/>
                <Button Content="打印" ToolTip="打印"/>
                <Button Content="导出" ToolTip="导出" Click="BtExport_Click"/>
                <Button x:Name="BtOption" Content="选项" ToolTip="选项" Click="BtOption_Click"/>
            </StackPanel>
        </Grid>
        <Grid>
            <DataGrid x:Name="DgGrid"
                      hc:DataGridAttach.ShowRowNumber="True"
                      hc:DataGridAttach.ShowSelectAllButton="False" 
                      Background="#FAFAFA"
                      Width="auto"
                      RowHeaderWidth="40">
            </DataGrid>
        </Grid>
    </DockPanel>
</UserControl>
