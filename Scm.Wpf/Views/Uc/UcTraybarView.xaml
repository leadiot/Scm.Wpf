<UserControl
    x:Class="Com.Scm.Wpf.Views.Uc.UcTraybarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    mc:Ignorable="d" 
    Height="24"
    d:DesignHeight="450" d:DesignWidth="800">
    <Border
        BorderBrush="{StaticResource ScmBorderBrush}"
        BorderThickness="1">
        <Border.Background>
            <LinearGradientBrush EndPoint="0,1" MappingMode="RelativeToBoundingBox" StartPoint="0,0">
                <GradientStop Color="#FFFFFF"/>
                <GradientStop Color="#EEEEEE"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid Margin="5 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="LcInfo" Grid.Column="0" Text="{Binding Path=Info}" HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
            <hc:Divider Grid.Column="1" Orientation="Vertical"/>
            <TextBlock x:Name="LcTime" Grid.Column="2" Text="{Binding Path=Time}" Padding="5"/>
        </Grid>
    </Border>
</UserControl>
