<UserControl x:Class="Com.Scm.Views.Uc.UcChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="450">
    <Border
        BorderBrush="{StaticResource ScmBorderBrush}"
        BorderThickness="1"
        CornerRadius="{StaticResource ScmCornerRadiusMd}"
        Margin="{StaticResource ScmSpacingMd}"
        Padding="{StaticResource ScmSpacingLg}"
        Effect="{StaticResource ScmShadowSm}">
        <Border.Background>
            <LinearGradientBrush EndPoint="0.5,1" MappingMode="RelativeToBoundingBox" StartPoint="0.5,0">
                <GradientStop Color="{StaticResource white}"/>
                <GradientStop Color="#EDFAF0" Offset="0.4"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>

        </Grid>
    </Border>
</UserControl>
