<UserControl
    x:Class="Com.Scm.Views.Config.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800"
    Background="{StaticResource ScmPanelBackground}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel>
            <Border Style="{StaticResource ScmCardStyle}" Margin="{StaticResource ScmSpacingMd}">
                <StackPanel>
                    <TextBlock Text="系统设置" Style="{StaticResource ScmTextTitle}" Margin="{StaticResource ScmSpacingMd}"/>
                    <Border BorderThickness="0 0 0 1" BorderBrush="{StaticResource ScmBorderBrush}" Padding="{StaticResource ScmSpacingSm}">
                        <DockPanel>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                <iconPacks:PackIconMaterial Kind="RocketLaunch" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource ScmColorPrimary}"/>
                                <TextBlock Text="自动启动" Style="{StaticResource ScmTextBody}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <CheckBox x:Name="CkAutoStart" HorizontalAlignment="Right" Foreground="{StaticResource ScmTextColorSecondary}" Click="CkAutoStart_Click">跟随系统启动</CheckBox>
                        </DockPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource ScmCardStyle}" Margin="{StaticResource ScmSpacingMd}" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="关于应用" Style="{StaticResource ScmTextTitle}" Margin="{StaticResource ScmSpacingMd}"/>
                    <Border BorderThickness="0 0 0 1" BorderBrush="{StaticResource ScmBorderBrush}" Padding="{StaticResource ScmSpacingSm}">
                        <DockPanel>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                <iconPacks:PackIconMaterial Kind="Information" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource ScmColorPrimary}"/>
                                <TextBlock Text="关于" Style="{StaticResource ScmTextBody}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <iconPacks:PackIconMaterial Kind="ChevronRight" HorizontalAlignment="Right" Foreground="{StaticResource ScmTextColorSecondary}" VerticalAlignment="Center"/>
                        </DockPanel>
                    </Border>
                    <Border BorderThickness="0 0 0 1" BorderBrush="{StaticResource ScmBorderBrush}" Padding="{StaticResource ScmSpacingSm}">
                        <DockPanel>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                <iconPacks:PackIconMaterial Kind="HelpCircle" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource ScmColorPrimary}"/>
                                <TextBlock Text="使用帮助" Style="{StaticResource ScmTextBody}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <iconPacks:PackIconMaterial Kind="ChevronRight" HorizontalAlignment="Right" Foreground="{StaticResource ScmTextColorSecondary}" VerticalAlignment="Center"/>
                        </DockPanel>
                    </Border>
                    <Border BorderThickness="0 0 0 1" BorderBrush="{StaticResource ScmBorderBrush}" Padding="{StaticResource ScmSpacingSm}">
                        <DockPanel>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                <iconPacks:PackIconMaterial Kind="Tag" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource ScmColorPrimary}"/>
                                <TextBlock Text="版本" Style="{StaticResource ScmTextBody}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock x:Name="TbVersion" HorizontalAlignment="Right" Foreground="{StaticResource ScmTextColorSecondary}" Text="1.0.0.0" VerticalAlignment="Center"/>
                        </DockPanel>
                    </Border>
                    <Border BorderThickness="0" BorderBrush="{StaticResource ScmBorderBrush}" Padding="{StaticResource ScmSpacingSm}">
                        <DockPanel>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                <iconPacks:PackIconMaterial Kind="CloudDownload" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource ScmColorPrimary}"/>
                                <TextBlock Text="检测更新" Style="{StaticResource ScmTextBody}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <iconPacks:PackIconMaterial Kind="ChevronRight" HorizontalAlignment="Right" Foreground="{StaticResource ScmTextColorSecondary}" VerticalAlignment="Center"/>
                        </DockPanel>
                    </Border>
                </StackPanel>
            </Border >
        </StackPanel>
    </ScrollViewer>
</UserControl>
