<UserControl x:Class="Com.Scm.Wpf.Views.Demo.DemoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid
            IsReadOnly="True"
            MaxWidth="600"
            Margin="0,16,0,0"
            Grid.Row="0"
            HeadersVisibility="All"
            RowHeaderWidth="60"
            AutoGenerateColumns="False"
            ItemsSource="{Binding Items}">
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Width="80" CanUserResize="False" Binding="{Binding Index}" Header="索引"/>
                <DataGridTemplateColumn Width="60" CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding ImgPath}" Width="32" Height="32" Stretch="Uniform"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Binding="{Binding Name}" Header="名称"/>
                <DataGridCheckBoxColumn Width="100" CanUserResize="False" Binding="{Binding IsSelected}" Header="选中"/>
                <DataGridTextColumn Binding="{Binding Remark}" Header="备注"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <TextBox x:Name="TbDemo" Text="{Binding Text}" AcceptsReturn="True" AcceptsTab="True" MaxLines="5"/>
            <Button x:Name="BtAppend" Click="BtAppend_Click" HorizontalAlignment="Stretch">Notice</Button>

            <Button x:Name="BtNotice" Click="BtNotice_Click" HorizontalAlignment="Stretch">Notice</Button>
            <Button x:Name="BtToast" Click="BtToast_Click" HorizontalAlignment="Stretch">Toast</Button>
            <Button x:Name="BtAlert" Click="BtAlert_Click" HorizontalAlignment="Stretch">Alert</Button>

            <hc:Pagination
                Name="PgPpp"
                MaxPageCount="10"
                PageIndex="{Binding PageIndex, Mode=TwoWay}"
                IsJumpEnabled="True"
                PageUpdated="Pagination_PageUpdated">
            </hc:Pagination>
        </StackPanel>
    </Grid>
</UserControl>
