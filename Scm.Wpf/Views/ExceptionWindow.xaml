<hc:Window
    x:Class="Com.Scm.Views.ExceptionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    mc:Ignorable="d"
    ShowInTaskbar="False"
    ResizeMode="NoResize"
    Icon="/Resources/logo.ico"
    WindowStartupLocation="CenterOwner"
    Title="异常"
    Height="240" Width="420">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Bottom" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button x:Name="BtAccept" Click="BtAccept_Click" Margin="0 12 0 0" Style="{StaticResource ButtonPrimary}">确认</Button>
        </StackPanel>
        <DockPanel>
            <Border DockPanel.Dock="Left">
                <iconPacks:PackIconMaterial Name="IpIcon" Kind="Alert" Foreground="Red" Margin="6" Width="24" Height="24" VerticalAlignment="Top" />
            </Border>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock
                    x:Name="TbTitle"
                    Grid.Row="0"
                    FontWeight="Bold"
                    Margin="0 6"
                    TextWrapping="Wrap">
                    112312
                </TextBlock>
                <TextBox
                    x:Name="TbTrace"
                    Grid.Row="1"
                    IsReadOnly="True"
                    AcceptsReturn="True"
                    AcceptsTab="True"
                    TextWrapping="Wrap"
                    VerticalContentAlignment="Top"
                    VerticalScrollBarVisibility="Auto">
                    12123
                </TextBox>
            </Grid>
        </DockPanel>
    </DockPanel>
</hc:Window>
