<Window x:Class="Com.Scm.Views.Image.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="WPF图片查看器"
        Height="700" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="_文件(F)">
                <MenuItem x:Name="OpenImage" Header="_打开(O)" Click="OpenImage_Click" InputGestureText="Ctrl + O"/>
                <Separator/>
                <MenuItem x:Name="ExitApp" Header="_退出(E)" Click="ExitApp_Click"/>
            </MenuItem>
            <MenuItem Header="_操作(O)">
                <MenuItem x:Name="ZoomIn" Header="_放大(+)" Click="ZoomIn_Click" InputGestureText="Ctrl + +"/>
                <MenuItem x:Name="ZoomOut" Header="_缩小(-)" Click="ZoomOut_Click" InputGestureText="Ctrl + -"/>
                <MenuItem x:Name="ResetZoom" Header="_重置大小" Click="ResetZoom_Click" InputGestureText="Ctrl + 0"/>
                <MenuItem x:Name="FitToWindow" Header="_适应窗口" Click="FitToWindow_Click"/>
                <Separator/>
                <MenuItem x:Name="RotateLeft" Header="逆时针旋转" Click="RotateLeft_Click"/>
                <MenuItem x:Name="RotateRight" Header="顺时针旋转" Click="RotateRight_Click"/>
                <Separator/>
                <MenuItem x:Name="PrevImage" Header="上一张" Click="PrevImage_Click" InputGestureText="←"/>
                <MenuItem x:Name="NextImage" Header="下一张" Click="NextImage_Click" InputGestureText="→"/>
            </MenuItem>
        </Menu>

        <!-- 图片显示区域（带滚动条） -->
        <ScrollViewer x:Name="imgScrollViewer" 
                      Grid.Row="1" 
                      Margin="0,25,0,30"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      MouseWheel="ImgScrollViewer_MouseWheel">
            <Image x:Name="imgDisplay" 
                   Stretch="None" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"/>
        </ScrollViewer>

        <!-- 底部信息栏 -->
        <StatusBar Grid.Row="2" Margin="0,0,0,0" Height="30">
            <TextBlock x:Name="txtImageInfo" VerticalAlignment="Center" Padding="5,0,0,0"/>
        </StatusBar>
    </Grid>
</Window>