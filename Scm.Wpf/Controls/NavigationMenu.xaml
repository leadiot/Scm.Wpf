<UserControl
    x:Class="Com.Scm.Wpf.Controls.NavigationMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dvo="clr-namespace:Com.Scm.Wpf.Dvo.Menu"
    mc:Ignorable="d"
    Background="#2C3E50"
    d:DesignHeight="600" d:DesignWidth="250">

    <UserControl.Resources>
        <SolidColorBrush x:Key="MenuBackgroundBrush" Color="#2C3E50" />
        <SolidColorBrush x:Key="MenuHoverBrush" Color="#34495E" />
        <SolidColorBrush x:Key="MenuActiveBrush" Color="#3498DB" />
        <SolidColorBrush x:Key="MenuTextBrush" Color="#ECF0F1" />
        <SolidColorBrush x:Key="MenuTextActiveBrush" Color="White" />

        <Style x:Key="MenuIconStyle" TargetType="Image">
            <Setter Property="Width" Value="20" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Margin" Value="0,0,10,0" />
        </Style>

        <Style x:Key="MenuExpanderStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource MenuTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid Background="Transparent">
                            <Path x:Name="Arrow" 
                                  Data="M0,0 L8,8 L16,0" 
                                  Fill="{TemplateBinding Foreground}" 
                                  HorizontalAlignment="Center" 
                                  VerticalAlignment="Center" 
                                  Width="16" 
                                  Height="16" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Arrow" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="90" CenterX="8" CenterY="8" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <HierarchicalDataTemplate
            x:Key="MenuTemplate" 
            DataType="{x:Type dvo:ScmMenuDvo}" 
            ItemsSource="{Binding children}">
            <Grid x:Name="MenuGrid" Height="48" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="40" />
                </Grid.ColumnDefinitions>

                <Image
                    Grid.Column="0"
                    Style="{StaticResource MenuIconStyle}"
                    Source="{Binding Icon}" />

                <TextBlock
                    Grid.Column="1" 
                    Text="{Binding Namec}" 
                    Foreground="{StaticResource MenuTextBrush}" 
                    VerticalAlignment="Center" 
                    FontSize="14" />

                <ToggleButton
                    Grid.Column="2" 
                    Style="{StaticResource MenuExpanderStyle}" 
                    IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=TreeViewItem}}"  />
            </Grid>

            <HierarchicalDataTemplate.ItemTemplate>
                <HierarchicalDataTemplate
                    DataType="{x:Type dvo:ScmMenuDvo}" 
                    ItemsSource="{Binding children}">
                    <Grid x:Name="SubMenuGrid" Height="40" Background="Transparent" Margin="20,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="30" />
                        </Grid.ColumnDefinitions>

                        <Image
                            Grid.Column="0" 
                            Style="{StaticResource MenuIconStyle}" 
                            Source="{Binding Icon}"  />

                        <TextBlock
                            Grid.Column="1" 
                            Text="{Binding Namec}" 
                            Foreground="{StaticResource MenuTextBrush}" 
                            VerticalAlignment="Center" 
                            FontSize="13" />

                        <ToggleButton
                            Grid.Column="2" 
                            Style="{StaticResource MenuExpanderStyle}" 
                            IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource AncestorType=TreeViewItem}}"  />
                    </Grid>
                </HierarchicalDataTemplate>
            </HierarchicalDataTemplate.ItemTemplate>
        </HierarchicalDataTemplate>

        <Style x:Key="MenuTreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Border
                                x:Name="Bd" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter
                                    x:Name="PART_Header" 
                                    ContentSource="Header" 
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource MenuHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource MenuActiveBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource MenuTextActiveBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <TreeView
            x:Name="MenuTree" 
            ItemsSource="{Binding Menus}" 
            ItemTemplate="{StaticResource MenuTemplate}" 
            Background="Transparent" 
            BorderThickness="0" 
            ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem" BasedOn="{StaticResource MenuTreeViewItemStyle}">
                    <EventSetter Event="Selected" Handler="TreeViewItem_Selected" />
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </Grid>
</UserControl>