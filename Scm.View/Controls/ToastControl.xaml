<UserControl x:Class="Com.Scm.Controls.ToastControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="300">
    <Border
        x:Name="BdPanel"
        CornerRadius="{StaticResource ScmCornerRadiusSm}"
        Margin="0, 0, 0, 12"
        Padding="15"
        Effect="{StaticResource ScmShadowMd}">
        <DockPanel>
            <Button
                x:Name="BtClose"
                Padding="0"
                Cursor="Hand"
                ToolTip="关闭"
                DockPanel.Dock="Right"
                Visibility="Collapsed"
                Click="BtClose_Click">
                <iconPacks:PackIconMaterial
                    Kind="Close"
                    Margin="6"
                    Foreground="{StaticResource ScmColorOnPrimary}"
                    VerticalAlignment="Center" />
            </Button>
            <TextBlock
                x:Name="TbMessage"
                Foreground="{StaticResource ScmColorOnPrimary}"
                FontSize="{StaticResource ScmFontSizeMd}"
                TextWrapping="Wrap"
                Text="12312312"
                Margin="0 5 0 5"
                VerticalAlignment="Center" />
        </DockPanel>
    </Border>
</UserControl>
